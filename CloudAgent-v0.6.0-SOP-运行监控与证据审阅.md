# CloudAgent v0.6.0 SOP：运行监控与证据审阅

## 1. 适用范围

本 SOP 用于云门工作室 CloudAgent 的运行监控、异常处置、分析结论审核、成员纠错和正式档案写入。

CloudAgent 负责收集和整理协作事实，不代替负责人进行纪律判断、品德判断或最终人员评价。

## 2. 运行监控 SOP

### 2.1 正常状态

负责人在“负责人管理仪表盘”查看：

- Agent 是否在线
- 最近心跳、最近收到消息、最近完成分析
- 待分析会话数、待重试批次数、永久失败数
- 各群监听状态和连续失败次数
- 飞书权限、模型连接状态
- 模型调用次数、Token 和预计费用
- 影子样本数、已审核数、精确率和验收状态

### 2.2 心跳超时

Agent 每次更新心跳后，飞书云端自动化等待 10 分钟并复核同一条心跳是否仍未变化。

确认超时后：

- 只私聊负责人一次
- 将运行状态标记为“离线”
- 将告警状态标记为“已告警”
- 不在任何项目群或团队群发送提醒

负责人依次检查：

1. supervisor 和 Agent 主进程是否存在
2. 飞书事件监听子进程是否存在
3. 飞书权限是否失效
4. 模型服务、余额或额度是否异常
5. 失败批次是否已进入重试队列

### 2.3 恢复

离线实例重新产生心跳后，飞书自动化只私聊负责人一次，并自动：

- 将告警状态恢复为“正常”
- 写入恢复时间
- 保留历史告警时间，便于追踪故障周期

### 2.4 高风险运行事件

“Agent 运行事件”新增严重级别为“高”的记录后，飞书自动化立即私聊负责人，并附事件记录入口。

高风险事件包括但不限于：

- 批次达到永久失败阈值
- 飞书关键权限失效
- 纳管群持续读取失败
- 模型调用持续失败或服务不可用

## 3. 分析结论分层

### 3.1 事实

事实必须同时满足：

- 能定位到真实飞书消息 ID
- 能打开原始消息链接或具有可复核的来源记录
- 结论正文不超出消息直接表达的内容
- 明确所属项目、来源群和证据时间范围

事实示例：某成员提交了 Demo 链接、发布了文档、汇报了完成项、确认了截止时间。

### 3.2 推断

推断是基于事实的解释，例如“任务可能延期”“协作主动性较强”。推断必须记录：

- 证据数量和批次数量
- 原始消息 ID 与链接
- 涉及时间范围
- 推断理由
- 置信度
- 审核状态

推断不得自动写入正式成员档案。

### 3.3 评价

工作风格评价只允许在周度批次中生成，不允许根据单条消息实时形成。

最低门槛：

- 覆盖至少 7 天
- 至少 3 条已经人工确认的事实
- 事实来自至少 2 个独立分析批次
- 明确使用“在当前样本中表现为”，不得写成稳定人格标签

## 4. 审核流程

1. Agent 写入“Agent 结论证据链”，默认状态为“待审核”。
2. 审核人打开原始消息链接，核对人物、项目、时间和上下文。
3. 审核人选择“确认”“修正”或“驳回”。
4. 需要修正时填写修正结论和原因，不覆盖原始 Agent 输出。
5. 只有确认后的事实可以进入后续工作风格周度评估。
6. 只有通过影子验收门槛的正式评价才能关联成员档案。

## 5. 成员纠错

成员可以对以下问题提出修正：

- 贡献归属错误
- 缺少共同贡献者
- 项目归属错误
- 断章取义
- 将建议误写为承诺
- 将群体成果误写为个人成果

争议内容在负责人确认前，不得进入成员总结、项目结论或正式档案。

## 6. 影子验收与正式上线

证据治理当前处于“影子模式”。满足以下条件后，负责人才能人工决定是否切换正式模式：

- 已审核样本不少于 30 条
- 事实 / 推断分类准确率不低于 95%
- 不存在高敏感结论越权写入成员档案
- 成员纠错流程可正常追踪

未达到门槛时，Agent 只生成待审核结论，不自动形成正式人员评价。

## 7. 隐私边界

- 运行错误、Token、费用和权限异常只对负责人可见
- 未确认推断和评价不向成员公开
- 不公开群成员未读名单
- 不进行品德、心理健康、政治倾向等敏感判断
- 不以消息数量、在线时长或回复速度单独评价贡献
- 不自动踢人、删消息、改群设置或执行审批

