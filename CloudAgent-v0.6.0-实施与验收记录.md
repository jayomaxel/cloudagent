# CloudAgent v0.6.0 实施与验收记录

## 1. 版本目标

本版本完成两个治理闭环：

1. 在飞书运营中台建立可观察、可告警、可恢复的 Agent 运行监控。
2. 将 Agent 输出拆分为事实、推断和评价，并建立可追溯证据链与人工审核门槛。

## 2. 代码与配置

- 版本：`0.6.0`
- 心跳间隔：5 分钟
- 心跳超时：10 分钟
- 失败重试阈值：3 次
- 证据治理模式：影子模式
- 工作风格观察窗口：7 天
- 工作风格最低证据：3 条已确认事实、2 个独立批次
- 正式模式门槛：30 条审核样本、95% 分类准确率
- 模型费用：仅在配置单价后估算，未配置时只展示 Token

## 3. 运营中台数据表

| 数据表 | Table ID | 用途 |
|---|---|---|
| Agent 运行状态 | `tblBs02T94NCHvW1` | 实例心跳、进程、积压、权限、模型和影子验收状态 |
| Agent 群监听状态 | `tblZATtR9sAgiYoX` | 各群监听、心跳、失败和待分析数量 |
| Agent 运行事件 | `tbll84M9NywEOrUz` | 模型调用、失败、权限异常、恢复和费用明细 |
| Agent 结论证据链 | `tblGyogQ7iUBdRDG` | 事实、推断、评价、原始消息和审核记录 |

迁移后再次执行 dry-run 的结果为：无待建表、无待加字段、无破坏性变更。

## 4. 仪表盘

已在“负责人管理仪表盘”增加 9 个组件：

1. CloudAgent 运行监控说明
2. 运行中 Agent
3. 待重试批次
4. 永久失败批次
5. 异常监听群
6. 待审核分析结论
7. 结论层级分布
8. 运行事件类型
9. 模型 Token 趋势

Dashboard ID：`blkNy5kVLfr1Evf9`。

## 5. 飞书云端自动化

| 自动化 | Workflow ID | 状态 | 通知范围 |
|---|---|---|---|
| Agent 心跳超时提醒 | `wkfJlkrTb0kRwZs9` | 已启用 | 仅私聊负责人 |
| Agent 恢复提醒 | `wkfyeQAdwdZPwyQ6` | 已启用 | 仅私聊负责人 |
| Agent 高风险运行事件提醒 | `wkfd8FSgULK8AcqQ` | 已启用 | 仅私聊负责人 |

心跳超时采用“心跳更新触发、飞书云端延迟 10 分钟、按实例 ID 和运行时长数值复核”的方式。运行时长是每次心跳递增的精确版本号，避免飞书日期字段按自然日比较造成误报。即使本机 Agent 后续退出，已启动的超时复核仍在飞书云端执行。

## 6. 历史数据迁移

- 历史结论回填：21 条
- 模型调用：0 次
- 回填策略：保守历史迁移
- 结论层级：全部标记为“推断”
- 审核状态：全部标记为“待审核”
- 是否直接写入正式成员评价：否

## 7. 自动化测试

- 全量测试：62 / 62 通过
- 新增覆盖：运行状态归一化、Token 统计、成本估算、事实门槛、工作风格门槛、正式档案关联门槛、心跳精确版本比较、私聊边界、v0.6 schema
- 配置 JSON 和 JavaScript 语法检查：通过

## 8. 线上验收

- supervisor 已以隐藏窗口启动
- Agent 主进程已由 supervisor 拉起
- 启动对账和历史补读期间持续写入 5 分钟心跳
- 告警状态在首次健康写入时初始化为“正常”
- 运行状态表已写入 `cloudagent-main`
- Agent 版本已写入 `0.6.0`
- 运行位置已写入“本机 supervisor”
- 5 个纳管群均已写入群监听状态
- 5 个纳管群当前监听状态均为“正常”
- 开发组保持多项目混合群，不被绑定为单一项目
- 未识别群保持“待归属项目”，不自动猜测项目归属
- 群聊中不发送运行监控消息

## 9. 权限验收

负责人已明确授权并完成高级权限启用。

- Reader、Editor 和普通用户均不可见负责人管理仪表盘
- Reader、Editor 和普通用户均不可见 Agent 运行状态
- Reader、Editor 和普通用户均不可见 Agent 群监听状态
- Reader、Editor 和普通用户均不可见 Agent 运行事件
- Reader、Editor 和普通用户均不可见 Agent 结论证据链
- Base 负责人保留管理和审阅权限
- 影子模式期间成员不可见未确认推断和评价
- 正式模式通过验收后，再建立“成员本人只读自己的已确认结论”角色

## 2026-08-07 运行时可靠性补充验收

本轮线上验收发现并修复了四个相互关联的恢复边界问题：

1. 重启时，尚未到期的持久化批次没有重新注册定时器，导致批次永久停留在待处理状态。现已统一由 `scheduleBatchRetry` 恢复剩余等待时间。
2. 消息索引和批次状态的首次飞书写入原位于可靠性 `try/catch` 之外，遇到 EOF 时不会生成下一次重试时间。现已纳入同一失败退避边界。
3. `start()` 失败后心跳定时器仍存活，导致 supervisor 无法识别主进程已经失效。现已增加启动失败清理，清除全部定时器和事件流后以非零状态退出。
4. 自动历史补读没有排除 `processed` 和 `inFlight` 消息，会重新构造同一批次并覆盖重试元数据。现已在补读入口去重，并在批次提交前登记 `inFlight`。

验收证据：完整测试集 66/66 通过；目标 AIMI 批次能够从失败状态写入未来重试时间；启动失败的 PID 50760 以 code=1 退出，supervisor 自动拉起 PID 28240；五个纳管群最近一次中台检查均为正常、连续失败为 0、永久失败为 0。飞书网络仍出现间歇性 EOF，Agent 按退避和 supervisor 机制自恢复。全程未向群聊发送测试或运维消息。
### 启动降级与最终运行状态补充

群成员首次同步被定义为可降级启动步骤：失败时记录错误并继续建立消息事件流，由周期同步兜底；消息事件流仍为启动必需项。完整测试集更新为 67/67。最终运行实例 PID 46228 已建立消息流和三个成员事件流。目标 AIMI 批次保持第 3 次重试及未来退避时间，未再被自动补读降级覆盖。飞书接口仍有间歇性 EOF，当前 9 个历史批次由本地可靠队列继续恢复，永久失败为 0。