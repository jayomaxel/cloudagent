# CloudAgent 历史消息补读

CloudAgent 支持对已配置的飞书群按时间范围补读历史消息，并沿用实时分析的写入流程。

## 默认范围

历史补读只允许以下三个群：

- 开发组
- 飞书云agent
- AIMI智能体设计

`云门智能体团队开发群` 被写入代码级排除名单，不会被补读，也不会进入分析。

## 使用方式

在项目目录运行：

```powershell
npm run backfill -- --start "2026-08-01T00:00:00+08:00" --end "2026-08-01T23:59:59+08:00"
```

只补读某一个群时，可以追加 `--chat-id`；多个群可以重复参数或使用逗号分隔：

```powershell
npm run backfill -- --start "2026-08-01T00:00:00+08:00" --chat-id oc_9134db54ac336d6aa8e5b21f0e18cbfa
```

不填写 `--end` 时，默认补读到命令执行时刻。`--start` 必须明确填写，避免误读过长历史并造成不必要的模型消耗。

## 安全与重复执行

- 补读使用已授权的飞书用户身份只读消息；实时监听仍使用机器人身份，不发送任何群消息。
- 只分析启用分析且位于允许名单中的群。
- 忽略机器人消息和过短消息。
- 已成功处理过的消息 ID 会保存到 `.data/processed-message-ids.json`，同一时间段重复执行时会跳过。
- 补读仍然写入贡献证据、行动项、决策记录和文档审核草稿，草稿是否创建飞书文档继续遵循群配置。
- 建议补读时暂时停止正在运行的旧版监听进程，完成后再启动最新版 Agent，避免旧进程与补读任务同时写入。
