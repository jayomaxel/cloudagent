# 云门工作室管理 Agent 路线图与验收标准

> 文档类型：实施路线图  
> 基线版本：0.3.2  
> 更新日期：2026-08-06

## 1. 建设目标

将现行群聊采集 Agent 逐步建设为可靠、可纠错、可追溯的工作室项目管理中枢，同时避免自动化越权和成员评价失真。

目标处理链路：

```text
消息和其他工作证据
→ 可靠接入与去重
→ 项目及运营归属
→ 结构化事实
→ 人工审核与成员纠错
→ 周报和月报
→ 正式项目库、成员档案和知识库
```

## 2. 当前完成度

| 能力 | 状态 | 说明 |
|---|---|---|
| 实时群聊监听 | 已完成 | 支持群消息事件流 |
| 30分钟对话轮次 | 已完成 | 实时与历史消息统一拆分 |
| 历史消息补读 | 部分完成 | 支持回补，缺少游标与完整增量机制 |
| 本地持久化去重 | 部分完成 | 可跨重启，缺少批次状态和多实例一致性 |
| 成员档案与群成员同步 | 已完成 | 支持新增、提醒、退群暂停和待确认事项 |
| 通知摘要与阅读追踪 | 已完成 | 5分钟成段、8小时一次提醒、最长7天 |
| 讨论响应记录 | 已完成 | 需要增加语义相关性判断 |
| 话题、贡献、行动和决策提取 | 已完成 | 仍需改进唯一 ID 和失败重试 |
| 开发组跨项目路由 | 未完成 | 当前仍是群级单项目映射 |
| 成员纠错闭环 | 未完成 | 只有待确认字段，没有成员提交入口 |
| 周报和月报引擎 | 未完成 | 尚无周期聚合和调度 |
| 成员周期观察 | 未完成 | 尚未按证据门槛生成 |
| 多来源贡献证据 | 未完成 | 当前以聊天为主 |
| 模型分工和成本控制 | 未完成 | 当前没有分层模型与成本记录 |
| 云端高可用运行 | 未完成 | 当前依赖单个 Node.js 长驻进程 |

## 3. P0：可靠性与安全基础

目标版本：0.4.0。

### 3.1 分析批次与失败重试

新建“Agent 分析批次”，至少记录：

- 批次 ID
- 群聊与对话轮次开始、结束时间
- 消息数量和消息 ID
- 模型、Agent 版本和处理状态
- Token 使用、重试次数和错误信息
- 创建时间、完成时间和最后重试时间

处理状态统一为：待处理、处理中、成功、失败、人工跳过。

只有结构化结果全部成功落库后，批次和消息才能标记为成功。失败批次按照固定退避策略重试，达到最大次数后进入人工处理视图。

### 3.2 唯一事实 ID

贡献、行动、决策、风险和文档草稿分别建立稳定 ID：

```text
事实ID = 项目 + 类型 + 主要成员/负责人 + 来源消息集合 + 内容指纹
```

同一条消息产生多个行动项或贡献时必须分别保存；同一成果在多个群重复汇报时可以关联为同一事实，但必须保留全部来源。

### 3.3 统一脱敏

所有模型调用前统一隐藏：

- 密码和验证码
- API Key、Access Token 和 Refresh Token
- 私钥和数据库连接串
- 账号口令和 Cookie
- 明确标记为不应分析的个人信息

脱敏发生在模型调用前，不依赖模型自行遵守提示词。

### 3.4 运行健康与恢复

- 增加 Agent 心跳、最近成功批次和最近错误状态。
- 防止同一配置同时启动两个消费实例。
- 支持按批次人工重放。
- 服务器重启后自动恢复监听、未完成批次和通知追踪。

### 3.5 P0 验收标准

- 模拟模型失败后，批次进入失败状态并自动重试。
- 模拟飞书写入部分失败后，不把消息标为全部完成。
- 同一消息产生两个行动项时，两条记录都存在。
- Agent 重启后不会重复生成已成功事实。
- 原始消息含测试密钥时，发送给模型的内容已被替换。
- 两个 Agent 实例不能同时消费同一套配置。

## 4. P1：项目归属与审核闭环

目标版本：0.5.0。

### 4.1 项目主数据

项目库统一维护：

- 正式项目名称
- 项目别名和历史名称
- 项目负责人和成员
- 项目群聊
- Git 仓库、Demo 和核心文档
- 项目状态、阶段、交付时间和风险状态

所有 Agent 记录使用项目库记录或稳定项目 ID，不再只使用自由文本项目名。

### 4.2 开发组多项目路由

判断顺序：

1. 汇报模板中的项目名称。
2. 正式项目名和别名。
3. 回复线程已经确认的项目。
4. 仓库、文档、Demo 和产品名称。
5. 发送者的项目成员关系。
6. 最近上下文。
7. 模型语义判断。
8. 人工确认。

一轮讨论允许产生多个项目归属，以及工作室运营、公共资源、培训和无法确认等归属。

### 4.3 成员纠错

新建“Agent 纠错申请”，字段至少包括：

- 申请成员
- 关联证据、报告或成员观察
- 纠错类型
- 原结论
- 建议修改
- 补充证据
- 提交时间
- 审核状态、审核人和审核说明

纠错类型包括成员归属错误、项目归属错误、遗漏共同贡献者、完成状态错误、讨论误判为决策、断章取义、风格误判和隐私问题。

### 4.4 审核状态机

统一状态：

```text
待确认 → 审核中 → 已确认
                  → 退回修订
                  → 已作废
```

只有“已确认”事实可以进入正式报告；存在未决纠错的事实暂停进入成员总结。

### 4.5 P1 验收标准

- 开发组中的标准 AIMI 汇报可以进入 AIMI 项目。
- 同一轮跨项目协调可以建立两个项目归属。
- 低于置信度门槛的内容只进入待归属视图。
- 成员能够提交一次贡献归属纠错并看到处理状态。
- 负责人可以查看来源消息后确认、修订或作废。
- 纠错未结束前，对应内容不会进入正式成员档案。

## 5. P1：周报与项目管理闭环

目标版本：0.6.0。

### 5.1 周报类型

- 项目周报
- 工作室运营周报
- 项目组合周报

周报采用北京时间自然周，每周一生成上一个自然周的待审核报告。

### 5.2 项目周报内容

- 执行摘要
- 目标与实际结果
- 有效交付
- 行动项闭环
- 决策和变更
- 风险与阻塞
- 成员贡献及共同贡献者
- 协作事实
- 下周计划和需要负责人介入的事项

每项关键结论必须关联已确认事实或来源证据。

### 5.3 运营和项目组合周报

运营周报聚合项目治理、成员接入、资源事项、制度建设、培训和管理者工作事实。

项目组合周报展示项目状态、关键进展、跨项目依赖、资源冲突、长期未更新项目和管理介入清单，不生成未经解释的综合分数。

### 5.4 行动项转任务

- Agent 提取的行动项先进入待确认。
- 负责人确认负责人、DDL 和项目后，可以转成飞书任务。
- 飞书任务状态变化应回写行动项。
- 任务取消、延期和变更应保留历史记录。

### 5.5 周报验收标准

- 同一项目群和开发组的重复汇报不会重复计算贡献。
- 无活动且无到期事项的项目不生成空周报。
- 周报中的交付、风险、决策和行动项均可追溯。
- 报告生成后只进入待审核状态，不自动进入正式知识库。
- 负责人确认行动项后可以创建飞书任务并同步状态。

## 6. P2：月报、多来源证据和成员成长

目标版本：0.7.0。

### 6.1 月报

月报优先聚合已审核周报、已确认贡献、行动项状态、项目库变化和公共资源记录。只有发现冲突或信息缺口时才回查原始消息。

月报包括：

- 项目月报
- 工作室运营月报
- 项目组合月报

### 6.2 多来源证据

按顺序接入：

1. 成员周成长记录。
2. 飞书任务完成记录。
3. 项目文档和知识库产出。
4. GitHub Commit、Issue 和 Pull Request。
5. Demo、部署和验收记录。

不同来源可以共同支持同一贡献事实，聊天记录不再是唯一依据。

### 6.3 成员周期观察

成员工作风格只根据已确认的可观察事实生成：

- 周度暂定倾向：至少 3 次独立行为，分布在不同日期。
- 月度稳定倾向：至少 5 次独立行为，跨越不少于 3 周。

输出必须包含观察周期、行为证据、适合场景、可能需要支持的场景、置信度、成员确认状态和负责人确认状态。

### 6.4 P2 验收标准

- 月报主要由已审核周报聚合，不重新分析全部原始聊天。
- 成员周成长提交可以关联成员、项目和报告周期。
- 同一贡献可以同时关联聊天、任务、文档和 GitHub 证据。
- 未达到证据门槛时只保存单次行为，不形成稳定风格结论。
- 成员可以查看、确认或纠正自己的周期观察。

## 7. P3：成本、质量评估和云端运行

目标版本：1.0.0。

### 7.1 模型分工

| 任务 | 推荐策略 |
|---|---|
| 去重、别名和模板匹配 | 规则，不调用模型 |
| 项目归属初判 | 规则优先，低成本模型补充 |
| 事实提取 | 低成本结构化模型 |
| 周报聚合 | 标准质量模型 |
| 月报、风险复盘和成员观察 | 高质量模型 |
| 高风险结论 | 高质量模型加人工审核 |

记录每个批次的输入、输出 Token、模型、耗时、失败和估算成本，并设置日、周、月预算提醒。

### 7.2 质量评估集

从历史消息中人工标注一组脱敏样本，覆盖：

- 已完成与计划的区分
- 多项目归属
- 共同贡献者
- 行动负责人和 DDL
- 讨论与正式决策
- 低证据工作风格观察
- 重复汇报
- 敏感信息

更换模型、提示词或 Schema 前必须运行回归评估。

### 7.3 云端部署

- 部署到持续在线的服务器或托管服务。
- 使用进程守护和自动重启。
- 密钥通过服务器环境变量或密钥管理服务注入。
- 日志不记录消息正文和密钥。
- 定期备份配置、批次状态和中台关键表。
- 提供健康检查和负责人故障提醒。

### 7.4 P3 验收标准

- 可以统计每个项目和报告周期的模型成本。
- 超出预算时暂停低优先级分析并提醒负责人。
- 回归样本中的项目归属、事实提取和敏感信息处理达到批准阈值。
- 服务器重启后 Agent 自动恢复且不会重复写入事实。
- Agent 连续运行期间能够检测并报告事件流中断。

## 8. 总体验收红线

以下任一情况发生时，版本不得标记为正式完成：

- 排除群消息进入模型或运营中台。
- 没有来源证据的贡献、风险或成员观察进入正式档案。
- 同一消息重启后重复生成正式事实。
- 成员无法查看或纠正与自己相关的结论。
- Agent 自动执行成员处分、权限限制或项目淘汰。
- 密钥、密码或验证码以明文进入模型输出、文档或日志。
- 周报、月报和 SOP 草稿未经审核直接进入正式知识库。

## 9. 建议实施顺序

1. 先完成 0.4.0 可靠性和安全基础。
2. 再完成 0.5.0 项目归属与纠错。
3. 在事实可信后建设 0.6.0 周报与任务闭环。
4. 之后接入多来源证据、月报和成员观察。
5. 最后进行模型成本优化和云端高可用建设。

周报、成员画像和管理仪表盘不能绕过前两阶段直接建设，否则只是把不可靠的事实包装得更完整。


## 2026-08-07 落地状态

| 优先级 | 能力 | 状态 | 验收说明 |
|---|---|---|---|
| P0 | 失败重试 | 已落地 | 1/5/15/60 分钟，第 5 次永久失败；支持重启恢复 |
| P0 | 唯一事实 ID | 已落地 | 贡献、行动、决策、草稿均使用稳定 ID |
| P0 | 消息索引 | 已落地 | 记录处理状态、指纹、批次、重试，不保存原文 |
| P0 | 敏感信息脱敏 | 已落地 | 持久化和模型调用双边界 |
| P1 | 开发组跨项目路由 | 影子运行 | 等待至少 3 天、30 条人工审核消息 |
| P1 | 正式名称与别名 | 已落地 | 4 个项目已写入确认别名，新增候选需人工采纳 |
| P1 | 自动正式切换 | 明确禁用 | 只能人工切换，确定性精度需达到 95% |

## 当前验收进度（2026-08-07）

- P0 可靠性和 P1 路由实现已完成，包含回复继承、历史正序回补和私聊审核闭环。
- 当前有效确定性影子路由为 1 条，已审核 0 条，观察时间不足 3 天。
- 因开发组全部可见历史仅 28 条，暂不能通过历史回补凑足 30 条；后续以真实新增消息和负责人审核自然积累。
- 在样本与时间门槛满足前，维持影子模式，禁止自动晋级。
