# CloudAgent v0.6.0：运行监控与证据治理

## 目标

本版本建立两个闭环：负责人能够在飞书运营中台确认 Agent 是否持续运行；所有项目分析结论都能区分事实、推断和评价，并追溯到真实消息。

## 运行监控

- Agent 每 5 分钟覆盖更新实例心跳，不产生重复状态行。
- 每个纳管群独立记录最近消息、最近分析、待处理数量和连续失败次数。
- 模型调用记录用途、耗时、Token、成功状态和可配置的预计费用。
- 飞书工作流独立检测 10 分钟心跳超时，Agent 宕机时仍能私聊负责人。
- 所有运行告警只私聊运营负责人，不发送群消息。

## 证据治理

- 事实是明确发生的提交、交付、决定、指派或承诺。
- 推断是风险、建议、可能关系和未确认下一步。
- 评价只能从已确认事实按周生成，至少需要 3 条事实和 2 个不同批次。
- 推断和评价必须人工审核，无法因高置信度绕过审核。
- 影子模式下，评价即使确认也不会自动关联成员档案。
- 禁止人格、品德、心理、天赋、能力排名和成员总分。

## 验收门槛

- 监控：真实心跳、群状态和模型事件写入成功；离线告警由独立工作流触发。
- 分层：至少人工审核 30 条结论，层级判定精确率不低于 95%。
- 正式入档必须由负责人手工将治理模式从 shadow 切换为 formal。

## 运行时恢复边界补充

历史补读、实时消息和失败恢复共享同一个消息占用集合。进入分析前必须同时满足：消息未在 `processed` 中、消息未在 `inFlight` 中；一旦批次提交，立即写入 `inFlight`。持久化批次重启后按原 `retryAt` 恢复定时器，不提前消耗模型额度。

启动过程采用失败即清理原则：任何未被显式降级处理的启动异常都会清理心跳、批次重试、通知定时器和事件流，再以非零状态退出，由 supervisor 按 1 秒、5 秒、15 秒、最长 60 秒的有界退避重启。这样中台不会出现“进程存在但实际未监听”的假在线状态。
### 启动降级与最终运行状态补充

群成员首次同步被定义为可降级启动步骤：失败时记录错误并继续建立消息事件流，由周期同步兜底；消息事件流仍为启动必需项。完整测试集更新为 67/67。最终运行实例 PID 46228 已建立消息流和三个成员事件流。目标 AIMI 批次保持第 3 次重试及未来退避时间，未再被自动补读降级覆盖。飞书接口仍有间歇性 EOF，当前 9 个历史批次由本地可靠队列继续恢复，永久失败为 0。