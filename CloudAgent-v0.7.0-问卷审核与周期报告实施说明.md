# CloudAgent v0.7.0 问卷审核与周期报告实施说明

## 本版能力

- 读取现有“成员周成长记录”，不创建重复问卷。
- 先做成员、项目、周期、重复和敏感信息确定性检查。
- 使用独立提示词完成第一层事实提取和第二层 AI 复核。
- A/B 级、低风险、无冲突事实可以自动写入运营中台。
- 无法确认、证据冲突、敏感评价和高风险事项进入负责人审核队列。
- 高风险事项立即私聊焦雪晴，普通待审核事项每天汇总一次。
- 不在群内公开催办、未提交名单或争议内容。
- 自动写入保存操作 ID、目标记录、写入前后快照和回滚状态。
- 生成成员、项目、工作室三个层级的周报和月报。

## 上线保护

迁移时，已有问卷会被标记为“历史待处理”，不会自动调用模型或发送提醒。新提交且“AI审核状态”为空的记录才由运行中的 Agent 自动处理。

历史回补先执行：

```powershell
npm.cmd run backfill:weekly -- --from 2026-08-01 --to 2026-08-31 --dry-run
```

确认范围和预计模型调用次数后再执行：

```powershell
npm.cmd run backfill:weekly -- --from 2026-08-01 --to 2026-08-31 --apply
```

默认不发送历史审核提醒；只有显式增加 `--notify` 才允许提醒。

## 人工审核

负责人在“Agent 负责人审核队列”中选择：确认、修改后确认、要求补充、暂缓、驳回、标记共同贡献或调整项目。Agent 定时同步决定，只有“确认”和“修改后确认”可以进入正式写入。

## 回滚

先预览：

```powershell
npm.cmd run rollback:write -- --operation-id write_xxx
```

确认精确操作 ID 后执行：

```powershell
npm.cmd run rollback:write -- --operation-id write_xxx --apply
```

更新操作恢复写入前快照；新建操作删除由该操作创建的目标记录。回滚结果继续保留在“Agent 自动写入日志”。

## 周期规则

- 时区：Asia/Shanghai。
- 周报：周一 08:00 后生成上一自然周。
- 月报：每月 1 日 09:00 后生成上一个自然月。
- 报告 ID 幂等，同一对象同一周期只生成一次。
- 包含高风险或未解决争议的报告进入“待负责人审核”。

