# CloudAgent v0.6.1 可靠性修复说明

## 1. 本版范围

本版只处理运行可靠性和自动接入群元数据，不实施周期分析、周报或月报。

## 2. 飞书 API 安全重试

`LarkClient.runJson()` 对飞书临时网络错误增加底层重试，默认等待时间为 500ms、1500ms、5000ms。

允许底层重试的操作：

- 记录、群消息、群成员和群资料读取。
- 按记录 ID 执行的批量更新。
- 带幂等键的消息和任务创建。

禁止底层自动重试的操作：

- Base 批量创建记录。
- 没有幂等键的消息发送。
- 无法证明重复执行安全的其他写操作。

非幂等创建继续由上层稳定事实 ID、批次恢复和落库前去重负责，避免请求已经成功但响应发生 EOF 时重复创建数据。

## 3. 事件消费者自动恢复

飞书事件消费者首次启动失败时仍由 supervisor 重启整个 Agent。消费者已经就绪后如果退出，则由 `LarkClient` 按 1 秒、5 秒、15 秒、60 秒自动重新拉起，恢复成功后重置退避计数。

Agent 主动停止时会关闭标准输入并清除重启定时器，不会误拉起已经停止的消费者。

覆盖事件：

- `im.message.receive_v1`
- `im.chat.member.user.added_v1`
- `im.chat.member.user.deleted_v1`
- `im.chat.member.bot.added_v1`

## 4. 自动接入群名称补全

当群聊配置中的群名仍等于 `oc_...` 群 ID 时，Agent 会读取飞书真实群资料并更新：

- 配置名称
- 群聊名称
- 最近同步时间

读取失败后 30 分钟再试。该流程不会自动修改所属项目，项目继续保持“待归属项目”，直到负责人确认。

## 5. 安全边界

- 不因本次修复增加任何群消息。
- 不更改群成员、群设置或权限。
- 不自动判断未知群所属项目。
- 不对非幂等创建进行网络层盲目重试。
- 运行告警仍只私聊负责人。

## 6. 版本

- 软件版本：`0.6.1`
- 配置版本：`0.6.1`
- 周期报告能力：仅完成方案设计，尚未实施。
