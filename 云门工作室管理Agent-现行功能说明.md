# 云门工作室管理 Agent 现行功能说明

> 文档类型：随版本更新的运行说明  
> Agent 配置版本：0.3.2  
> 更新日期：2026-08-06

## 1. 当前定位

现行 Agent 已具备群聊事实采集、历史补读、成员治理、通知追踪和部分飞书自动化能力。它目前属于“群聊事实采集 Agent + 团队运营自动化”，尚未形成完整的周报、月报和项目组合管理闭环。

## 2. 技术组成

| 模块 | 当前实现 |
|---|---|
| 运行环境 | Node.js 20+ 长驻进程 |
| 飞书接入 | lark-cli 与飞书事件流 |
| AI 接口 | OpenAI 兼容接口 |
| 默认模型供应商 | DeepSeek |
| 输出校验 | Zod 结构校验 |
| 业务数据 | 飞书多维表格 |
| 文档草稿 | 飞书云文档 |
| 本地状态 | `.data` 中的消息 ID 和提醒状态文件 |

当前运行依赖一台持续在线的计算机或服务器。进程在个人电脑运行时不等于云端服务；电脑关机、休眠或网络中断会停止实时监听。

## 3. 当前群聊范围

### 3.1 硬排除群

- 云门智能体团队开发群

该群 ID 同时受隐私排除配置保护，不进入实时分析和历史补读。

### 3.2 当前纳管群

- 开发组：团队总群和跨项目混合群
- 飞书云agent：项目群
- AIMI智能体设计：AIMI智能体项目群
- 五洲项目：项目群

实际分析群以“Agent 群聊配置”中已启用记录为准。本地成员治理配置用于成员同步，不能替代正式的群聊项目归属配置。

## 4. 当前消息处理流程

```text
飞书实时消息或历史消息
→ 排除机器人、短消息、重复消息和排除群
→ 按群聊缓存
→ 群内连续 30 分钟没有新消息
→ 将本轮对话发送给模型分析
→ 写入话题、贡献、行动、决策和文档草稿
```

当前配置最多允许一轮缓存 500 条消息。`maxMessages` 目前不是立即分析触发条件，是否结束一轮以 30 分钟无新回复为准。

## 5. 当前分析产物

### 5.1 群聊话题快照

每轮最多生成 3 个话题快照，记录：

- 一句话总结
- 明确进展
- 当前卡点
- 待办事项
- 待确认问题
- 后续建议
- 相关成员和来源消息

话题快照默认进入待确认状态，不替负责人作正式项目结论。

### 5.2 贡献证据

当前贡献类型包括：

- 技术实现
- 项目推进
- 产品需求
- 协作支持
- 知识沉淀
- 风险担当
- 组织贡献

每条证据记录成员身份状态、项目关系、置信度、来源消息和是否需要人工复核。现行 Agent 不会自动将证据写入正式成员总结。

### 5.3 行动项

Agent 可以从聊天中提取行动内容、负责人、截止日期和来源消息。提取结果写入“Agent 行动项”，状态为待确认，不自动转成正式飞书任务。

### 5.4 决策记录

Agent 可以提取决策内容、原因、参与成员和来源消息，默认进入待确认状态。

### 5.5 文档草稿

当全局和群聊同时允许文档草稿时，Agent 可以创建：

- 项目周报草稿
- 决策记录
- 技术知识
- 项目复盘
- SOP 修订建议
- 成员成长观察

当前知识库父级 Token 为空，在线文档不会自动进入指定知识库目录。

## 6. 历史补读

- 支持按群聊和时间范围分页读取历史消息。
- 历史消息按时间排序后，以 30 分钟间隔拆成对话轮次。
- 启动时自动补读最近 2 天。
- 支持通过命令手动补读更长时间范围。
- 排除群不会进入补读。
- 已处理消息 ID 会写入本地文件，Agent 重启后继续用于去重。

当前没有保存飞书历史游标和每个群的最近同步位置，启动自动补读也不是完整的全量增量同步系统。

## 7. 成员与项目关系治理

当前支持：

- 定时同步纳管群成员。
- 新成员自动建立待补全成员档案。
- 项目群新成员自动建立待确认项目关系。
- 资料不完整时私聊发送成员信息登记入口。
- 成员退出项目群时将关系调整为“暂停”。
- 为负责人建立成员或项目关系确认事项。
- 定期重新匹配贡献证据中的成员档案和项目关系。

当前成员身份来源是“成员档案 + 项目成员关系”。工作室成员、项目成员、指导教师、外部协作者和临时参与者不会被默认视为同一种身份。

## 8. 通知摘要与阅读追踪

焦雪晴发送带通知标志的内容后，连续 5 分钟没有新的通知消息，Agent 会：

1. 汇总这一轮通知。
2. 在原群发送一条面向成员的通知摘要。
3. 记录摘要消息 ID 和通知来源消息。
4. 定时同步群成员的摘要已读状态。
5. 超过 8 小时仍未读时私聊提醒一次。
6. 最长跟踪 7 天。

提醒不会公开未读名单，不要求回复“收到”，也不会重复提醒同一成员同一通知。

阅读追踪针对 Agent 发送的通知摘要，不等同于精确判断成员阅读了每一条原始通知。

## 9. 讨论响应记录

Agent 当前支持：

- 记录引用回复的响应时间。
- 记录不同成员连续发言时的顺序接话时间。
- 超过配置阈值后标记为超时。

该功能只能作为沟通流程参考。顺序接话可能与上一条消息无关，因此响应时间不能直接用于成员评价或绩效判断。

## 10. 飞书任务和日程

只有配置中的授权操作人可以使用明确确认指令：

- `【确认建任务】`
- `【确认建日程】`

任务指令必须包含任务、负责人和截止时间；日程指令必须包含标题、带时区的开始时间和结束时间。信息不完整时只建立负责人待确认事项，不执行创建。

## 11. 当前写入的运营中台表

- 成员档案
- 项目成员关系
- Agent 群聊配置
- Agent 贡献证据
- Agent 行动项
- Agent 决策记录
- Agent 文档审核
- Agent 群聊话题快照
- Agent 通知追踪
- Agent 通知阅读明细
- Agent 讨论响应监控

成员周成长记录已经建立，但当前尚未被 Agent 纳入贡献和成员周期分析。

## 12. 已知限制

### 12.1 可靠性

- 分析批次没有独立状态表和失败队列。
- 模型或飞书写入失败后，本轮不会自动按批次重试。
- 本地消息 ID 集合最多保留 10000 条，尚未按配置的保留天数清理。
- 多实例同时运行时没有分布式锁和统一去重。

### 12.2 数据归属

- 每个群映射仍只有一个默认项目。
- 开发组尚未实现逐消息、多项目和工作室运营路由。
- `project_summary` 已由模型生成，但当前没有落库。
- 同一成果跨群重复汇报尚未进行语义去重。

### 12.3 数据完整性

- 贡献、行动项和决策主要按来源消息 ID 去重，同一来源产生多条同类事实时可能被合并或遗漏。
- 当前主要证据来自聊天，没有接入 GitHub、任务、文档编辑和周成长提交。
- 图片 OCR、音视频转写和附件正文尚未进入分析。

### 12.4 报告与成员观察

- 尚未实现真正按自然周和自然月聚合的报告引擎。
- 尚未实现项目周报、运营周报和项目组合周报的定时生成。
- 尚未实现成员周期工作风格观察和证据门槛。
- 尚未实现成员纠错提交和负责人审核闭环。

### 12.5 成本与安全

- 当前没有按任务选择不同模型。
- 没有记录单批 Token、成本和重试次数。
- 一轮消息过长时没有分块分析与上下文预算。
- 通知摘要有敏感信息隐藏要求，但普通项目分析尚缺统一的模型调用前脱敏层。

## 13. 版本更新要求

每次发布新版本时，应同步更新：

- Agent 配置版本
- `package.json` 软件版本
- 本文档的实际能力和已知限制
- 新增权限和主动消息行为
- 新增或变更的运营中台表
- 历史数据迁移和回滚说明


## v0.5.0 当前能力补充

- 可靠批次：本地持久化、失败重试、重启恢复，成功后才标记消息已处理。
- 事实去重：贡献、行动、决策、草稿使用稳定唯一 ID。
- 安全处理：持久化和模型调用前脱敏，消息索引不保存原文。
- 影子路由：开发组支持一条消息归属多个项目；现阶段不改变正式分析结果。
- 人工复核：AI 语义归属始终待确认，负责人每两小时收到一条私聊汇总，群内不提醒。
- 别名治理：负责人采纳后同步项目库，不自动覆盖既有别名。
- 当前状态：v0.5.0 正在运行；3 天且至少 30 条人工审核消息后再评估正式切换。

## v0.5.1 运行补充

- 模型请求具备硬超时，网络异常进入统一重试，不会无限停留“处理中”。
- 语义路由失败不阻塞主分析；模型伪造或占位消息 ID 自动驳回。
- 实时与历史补读重叠消息自动去重，并对账中台状态。
- 使用 `npm run shadow-audit` 查看影子验收进度。
- 使用 `npm run agent:supervised` 长期运行，子进程退出后自动恢复。
